# mazedyn


Setup for graph embeddings (mac m3):
* make conda-osx-arm.lock
* conda create --name mazedyn-test --fil conda-osx-arm64.lock
* conda activate mazedyn-test
* make poetry.lock
* poetry add notebook

* conda install pydot